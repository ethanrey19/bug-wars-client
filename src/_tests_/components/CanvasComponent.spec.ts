// import { describe, expect, it, vi, beforeEach } from 'vitest';
// import { mount, shallowMount } from '@vue/test-utils';
// import CanvasComponent from '@/components/CanvasComponent.vue';

// describe('CanvasComponent.vue', () => {
//     beforeEach(() => {
//         vi.resetAllMocks();
//     });

//     it('Displays map when page is loaded', async () => {
//         // const spy = vi.spyOn(CanvasComponent, 'drawMap');
//         const wrapper = shallowMount(CanvasComponent)

//         const drawMapSpy = vi.spyOn(wrapper, 'drawMap')
//         expect(drawMapSpy).toHaveBeenCalled()

//     })
// })